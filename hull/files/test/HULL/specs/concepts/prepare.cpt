# Set default kind and test case to <kind>

* Fill data store with kind <kind> and case <kind>

# Prepare test case <case> for kind <kind> with test chart <chart> and values file <values_file> including suites <suites>

* Fill data store with kind <kind>, case <case>, chart <chart> and suites <suites>
* Clean the test execution folder
* Copy folders to test execution folder

# Prepare default test case for this kind with test chart <chart> and values file <values_file> including suites <suites>

* Fill data store with chart <chart> and suites <suites>
* Clean the test execution folder
* Copy folders to test execution folder

# Prepare test case <case> for kind <kind> with test chart <chart> and values file <values_file>

* Prepare test case <case> for kind <kind> with test chart <chart> and values file <values_file> including suites ""

# Prepare test case <case> for kind <kind> and values file <values_file>

* Prepare test case <case> for kind <kind> with test chart "hull-test" and values file <values_file> including suites ""

# Prepare test case <case> for kind <kind> and values file <values_file> including suites <suites>

* Prepare test case <case> for kind <kind> with test chart "hull-test" and values file <values_file> including suites <suites>

# Prepare test case <case> for kind <kind> including suites <suites>

* Prepare test case <case> for kind <kind> with test chart "hull-test" and values file "values.hull.yaml" including suites <suites>

# Prepare test case <case> for kind <kind>

* Prepare test case <case> for kind <kind> with test chart "hull-test" and values file "values.hull.yaml" including suites ""

# Prepare default test case for kind <kind> including suites <suites>

* Prepare test case <kind> for kind <kind> with test chart "hull-test" and values file "values.hull.yaml" including suites <suites>

# Prepare default test case for kind <kind>

* Prepare test case <kind> for kind <kind> with test chart "hull-test" and values file "values.hull.yaml" including suites ""

# Prepare test case <case> for this kind including suites <suites>

* Prepare test case <case> for this kind with test chart "hull-test" and values file "values.hull.yaml" including suites <suites>

# Prepare default test case for this kind including suites <suites>

* Prepare default test case for this kind with test chart "hull-test" and values file "values.hull.yaml" including suites <suites>

# Lint and Render to namespace <namespace>
* Lint and Render values file "values.hull.yaml" to namespace <namespace>


# Lint and Render
* Lint and Render to namespace "default"

# Lint and Render values file <values_file>
* Lint the templates for values file <values_file> to namespace "default"
* Render the templates for values file <values_file> to test execution folder and namespace "default"
* Fill data store with rendered objects

# Lint and Render values file <values_file> to namespace <namespace>
* Lint the templates for values file <values_file> to namespace <namespace>
* Render the templates for values file <values_file> to test execution folder and namespace <namespace>
* Fill data store with rendered objects

